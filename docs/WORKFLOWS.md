# Workflows

Workflow intent is documented independently from GitHub Actions implementation.

| Workflow | Purpose |
|---|---|
| [`ci-build.md`](workflows/ci-build.md) | Restore, build, typecheck, and test the repository on pushes and pull requests |
| [`publish-nuget.md`](workflows/publish-nuget.md) | Build, pack, and publish versioned NuGet packages from validated releases |
