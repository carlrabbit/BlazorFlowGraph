# Repository Tooling

## Purpose

This document describes the shared tooling scripts and configuration used across the monorepo.

## Contents

- `tooling/scripts/validate-samples-registry.sh` — validates deterministic sample registry and port invariants
- `tooling/scripts/run-samples-all.sh` — validates, builds, and launches all registered samples concurrently

## Authority

This document is authoritative for the repository tooling directory contents and purpose.

## Document Contract

Update this document when tooling scripts or utilities are added, removed, or renamed. Keep it synchronized with [`../ENGINEERING.md`](../ENGINEERING.md) and [`../../tooling/`](../../tooling/).
