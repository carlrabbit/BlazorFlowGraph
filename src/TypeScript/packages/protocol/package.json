{
  "name": "@dataflow-visualizer/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "DTOs, graph contracts, and diff contracts for the dataflow visualizer",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target browser --format esm --sourcemap=external && tsc -p tsconfig.json --emitDeclarationOnly",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests src/",
    "test:watch": "bun test --watch src/"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
