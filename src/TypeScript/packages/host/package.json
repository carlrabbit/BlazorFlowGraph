{
  "name": "@dataflow-visualizer/host",
  "version": "0.1.0",
  "private": true,
  "description": "Runtime bootstrap for the dataflow visualizer",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir ./dist --format esm --target browser --sourcemap=external --external @dataflow-visualizer/interop --external @dataflow-visualizer/layout --external @dataflow-visualizer/protocol --external @dataflow-visualizer/renderer-svg --external @dataflow-visualizer/runtime && bun build src/browser.ts --outdir ./dist --format iife --target browser --sourcemap=external && tsc -p tsconfig.json --emitDeclarationOnly",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@dataflow-visualizer/interop": "workspace:*",
    "@dataflow-visualizer/layout": "workspace:*",
    "@dataflow-visualizer/protocol": "workspace:*",
    "@dataflow-visualizer/renderer-svg": "workspace:*",
    "@dataflow-visualizer/runtime": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  }
}
