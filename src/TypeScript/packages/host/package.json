{
  "name": "@dataflow-visualizer/host",
  "version": "0.1.0",
  "private": true,
  "description": "Runtime bootstrap for the dataflow visualizer",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "vite build && vite build --config vite.browser.config.ts && tsc -p tsconfig.json --emitDeclarationOnly",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@dataflow-visualizer/interop": "workspace:*",
    "@dataflow-visualizer/layout": "workspace:*",
    "@dataflow-visualizer/protocol": "workspace:*",
    "@dataflow-visualizer/renderer-svg": "workspace:*",
    "@dataflow-visualizer/runtime": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.3"
  }
}
