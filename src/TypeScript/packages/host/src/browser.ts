/**
 * Browser bundle entry — auto-registers DataflowVisualizer on the window.
 * Import this module via a <script type="module"> tag in your HTML page.
 */

export { registerGlobals } from "./index";

import { registerGlobals } from "./index";
registerGlobals();
