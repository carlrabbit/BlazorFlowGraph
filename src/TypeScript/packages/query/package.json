{
  "name": "@dataflow-visualizer/query",
  "version": "0.1.0",
  "private": true,
  "description": "Topology query engine for the dataflow visualizer",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target browser --format esm --sourcemap=external -e @dataflow-visualizer/protocol -e @dataflow-visualizer/runtime && tsc -p tsconfig.json --emitDeclarationOnly",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests src/",
    "test:watch": "bun test --watch src/"
  },
  "dependencies": {
    "@dataflow-visualizer/protocol": "workspace:*",
    "@dataflow-visualizer/runtime": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
