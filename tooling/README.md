# Tooling

This directory contains shared tooling scripts and configuration used across the monorepo.

## Contents

- CI helper scripts
- Code generation tools (planned)
- Build utilities
- `scripts/validate-samples-registry.sh` — validates deterministic sample registry and port invariants
- `scripts/run-samples-all.sh` — validates, builds, and launches all registered samples concurrently
