# Concepts

BlazorFlowGraph is built around:

- semantic model (domain graph state)
- projection (render-ready graph)
- snapshot/diff synchronization
- browser runtime reconciliation
- overlays and diagnostics

Internal behavioral authority remains in `docs/specs/`.
