# Diagnostics

Diagnostics guidance for consumers:

- check browser console/runtime diagnostics
- verify package versions are compatible
- verify static web assets load from `_content/BlazorFlowGraph.Blazor/`

Diagnostic detail pages live under `public-docs/diagnostics/`.
