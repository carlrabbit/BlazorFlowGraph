{
  "name": "dataflow-visualizer",
  "private": true,
  "workspaces": ["src/TypeScript/packages/*", "tests/TypeScript/*"],
  "trustedDependencies": ["@biomejs/biome", "esbuild"],
  "scripts": {
    "check": "biome check .",
    "format": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
